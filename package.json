{
  "name": "ssrf-accounting",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && python -m uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
  },
  "engines": {
    "node": ">=20.0.0",
    "python": ">=3.9.0"
  }
}
