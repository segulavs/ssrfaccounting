[phases.setup]
nixPkgs = ["python3", "nodejs-18_x", "pip"]

[phases.install]
cmds = [
  "cd backend && pip install -r requirements.txt",
  "cd frontend && npm install && npm run build"
]

[phases.build]
cmds = []

[start]
cmd = "cd backend && python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
